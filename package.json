{
  "name": "realtime-subtitle-frontend",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "golden": "node scripts/generate-frontend-golden.js",
    "golden:compare": "node scripts/generate-frontend-golden.js compare",
    "golden:check": "node scripts/generate-frontend-golden.js check"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
