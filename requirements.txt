websockets
soundcard
numpy<2.0.0
ten-vad
# Optional alternative VAD backend (not bundled in the packaged exe).
# Install this and set DEFAULT_VAD_BACKEND="silero" in audio_router.py to switch.
# silero-vad-lite==0.2.1
python-dotenv
arabic-reshaper
python-bidi
aiohttp
aiohttp-jinja2
requests
pywebview
python-osc
zeroconf
PySide6
defusedxml
vrchat_oscquery @ git+https://github.com/theepicsnail/vrchat_oscquery@main
# streamlink
# 锁版本：不同 PyInstaller 版本的 bootloader 有不同的杀软误报情况，
# 浮动版本会让"这次报毒/上次不报"变得无法复现。升级时请手工改这里。
pyinstaller==6.22.2
